# Serverless — Azure Functions (exemplo)

1. Publique `serverless/azure-functions/log-event` em uma Function App Node 18.
2. Copie a URL (com `?code=...`) e substitua em `app.js` a constante `SERVERLESS_URL`.
3. Eventos enviados: `solve`, `hint`.
